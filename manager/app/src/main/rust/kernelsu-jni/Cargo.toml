[package]
name = "kernelsu-jni"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "kernelsu"

[dependencies]
jni = "0.21"
log = "0.4"
android_logger = "0.14"
libc = "0.2"

[profile.release]
lto = true
strip = true
opt-level = "z"
codegen-units = 1
panic = "abort"
